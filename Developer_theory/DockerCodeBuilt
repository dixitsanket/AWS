-------------------------------------------------------------------------------------------------------------------------------------------------------

								        DOCKER AND CODEBUILT
									--------------------

-------------------------------------------------------------------------------------------------------------------------------------------------------

Docker is an open source technology which allows you to create application based on either linux or windows containers

A Container is a lightweight standalone executable software package which includes everything the software need to run code, runtime environment,libraries,environment setting etc.

AWS provides Elastic Container Service as fully managed clustered platform which allows you to run your docker images in the cloud

AWS CodeBuild is a fully managed build service which runs a set of command that you define, e.g compile code runs test and produces artifacts that are ready to deploy.
